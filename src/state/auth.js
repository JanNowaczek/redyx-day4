const initialState = {
    user: null,
    email: 'email',
    password: 'password',
  }
  
  export default (state = initialState, action) => {
    switch(action.type){
      
      default:
        return state
    }
  }